# Failure case: <short slug, e.g. `org-switcher-shows-stale-role`>

- **Area:** <which prompt file found this, e.g. `07-login-routing-multi-org`>
- **Account used:** <gmail id>
- **Severity:** blocker | major | minor
- **Status:** OPEN

## Steps to reproduce

1. …
2. …
3. …

## Expected result

<what should have happened, per the prompt file's Pass criteria>

## Actual result

<what actually happened — be specific: wrong URL, wrong screen, wrong data
shown, error toast, console error text, etc.>

## Evidence

<console errors, network request/response details, or a plain description of
what was on screen — Claude in Chrome can't attach real screenshots to this
file, so describe what was visible instead>

## Retest history

| Date | Result | Notes |
|---|---|---|
| <date first found> | FAIL | initial finding |

<Add one new row per retest. When a retest comes back PASS, add a row with
Result = RESOLVED and change Status above to CLOSED — don't delete this file
even after it's fixed; it's the record that this was once broken.>

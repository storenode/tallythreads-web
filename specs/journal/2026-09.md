# Journal — September 2026

## 2026-09-05 — Purchase-Trip: multi-leg route, manual budgets, map-link, Claude-for-estimates
**Did:** Updated `purchase-trips.md` (v0.3.0), `backlog.md`, `schema.md`, journal — design
decisions only, no code yet (docs-before-code, founder's process).
**Decisions:**
- Route = **multi-leg table** (From/To/Boarding/Map-link/Price/Mode, add/edit/delete) in
  `route` jsonb (no migration).
- **All budgets manual** in planning (goods + estimated expenses); **actual expenses at trip
  time** (`trip_expenses`); a **planned-vs-actual** panel compares them.
- **Map** = pasted Google-Maps link (free text). **No** Google Places autocomplete, **no**
  hotel discovery (Google can't give real ₹ prices anyway) → both parked in `backlog.md`.
- **Claude/Haiku** does budget **estimates in Phase 2** (server-side, ~$5/mo cap, usage-based
  not a license). Adopt the API key only when that feature is built.
- **Map approach finalized (v0.3.1):** no Google key / no Places / no paste-parse. Each leg
  generates a **free Google "Maps URLs" verify-link** from From/To/Mode; owner clicks to
  eyeball (human typo-detection). Leg shape `{from,to,boarding,mode,price_paise}`. Photon/
  Places autocomplete = deferred auto-correction upgrade in `backlog.md`.
**Next:** implement the route-legs table + verify-link + planned-vs-actual (Phase 1) on
founder's go — see the scope handed over this session.
**Open:** sync cadence/interval; suppliers-master timing; MRP→sales (M5) seam.

## 2026-09-05 — New module M10 (Shift & Store Operations Log) added to scope
**Did:** Amended `constitution.md` to v1.11.0 — added **M10 — Shift & Store Operations
Log** to the §5 roadmap (24h; total 417h→441h), §2.VI v1 scope, and a §3 clarification
that this is *not* the "staff commission" non-goal. Wrote the spec
`roadmap/shift-store-ops-log.md` (v0.1.0). Propagated M10 into `reference/schema.md` §7
and `roadmap/status.md`.
**Why:** founder insight — QueueBuster (Bandrip's current POS) doesn't capture in-shift
operations. Three gaps worth selling on: (1) working hours from login→logoff, (2)
petty-utility expenses with approval, (3) end-of-shift handover notes (AI-assisted later).
**Decisions:** In scope as M10; commission tracking stays out; AI note is server-side-only
and deferred to Phase 3; M10's hours+expenses feed M1d settlement's `deduct_expenses`.
**Next:** decide build order (M10 Phase 1 pairs naturally with M1d).
**Open:** offline online-first-vs-wait-for-M2; expense categories/caps; who approves
(seed `store_manager`?); shift auto-close rule; what exactly the AI note does.

## 2026-09-05 — Specs restructure + live-DB schema verification
**Did:**
- Reorganized `specs/` by purpose/rate-of-change: `reference/` (schema, roles-and-permissions,
  franchise-settlement), `roadmap/` (status, workflow, future/ai-studio), `journal/`, and a
  new `README.md` front door. Dropped `M`-prefixes from filenames. Added root `CLAUDE.md`
  pointing here.
- Connected to the live Supabase project (`gmmeaplomgotqtivevkg`) via `supabase db dump`
  (started Docker for it) and rewrote `reference/schema.md` (v3.0.0) to match the live DB as
  source of truth. Replaced the stale hand-drawn `supabase/M1-schema-diagram.svg` with a
  maintainable Mermaid ER diagram (embedded in schema.md + `supabase/schema.mmd`).

**Decisions:**
- Live DB is authoritative for schema; the doc is kept to match it, not vice-versa.
- Journal = one file per month, newest entry on top.

**Corrections the live DB forced (docs were wrong):**
- `stock_locations`, `stock_transfers`, `settlement_statements` do **not** exist yet (M1c/M1d)
  — the old docs + diagram listed them as if built. Moved to "not yet migrated."
- `settlement_rules` live has **`config` only** — the `plugin_id` hybrid column is a proposed
  M1d migration, not applied.
- Live tables the docs missed: `demo_scenarios`, `qa_test_cases`.
- `organizations` has a stored `registration_type` (independent/chain/franchise) **and** the
  derived `store_business_model` view — a real tension with the "never store the model"
  principle. Documented: behavior must read the view; `registration_type` is declared-intent.

**Next:** Have the Operations roadmap discussion (start point: M2 vs M4 vs M1d).
**Open:** Franchise gross basis (with-GST vs taxable value); whether `registration_type`
should be relaxed to informational-only.

## 2026-09-05 — Franchise settlement corrected + doc consolidation (earlier same day)
**Did:** Corrected Bandrip's royalty base to the **post-expense** owner balance (not
post-share remainder); made the settlement engine an explicit **hybrid** (data-driven recipe
+ coded-plugin fallback, no `eval`). Updated `franchise-settlement.md` (v2.0.0) and
`constitution.md` §2.V + §8 (v1.10.0). Consolidated 16 spec docs → 7.
**Decisions:** Royalty = 13% of post-expense balance; ₹3,00,000 cliff is strict `>`; Bandrip
Corporate does the Purchase-Trip and stock distribution, the franchise branch receives goods
and owes a monthly settlement.
**Next:** Restructure specs (done — see entry above).
**Open:** Same franchise gross-basis question.
